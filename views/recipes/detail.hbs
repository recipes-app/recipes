<div class="d-flex justify-content-center ">
  <div id="recipe-card " class="card col-10 mt-3 shadow p-3 mb-5 bg-body rounded">
    <div class="recipe-card-img card-img-top col-md-4" style="background-image: url('{{recipe.image}}')"
      class="w-100 rounded border"></div>
    <div class="recipe-card-info d-flex align-items-center">
      <p class="card-text mt-4">{{recipe.keyWords}}</p>
      <h1 class="card-title">{{recipe.title}}</h1>
      <div>
        <p class="card-text mt-4"><strong>Author:</strong>{{recipe.author.name}}</p>
      </div>

    </div>
  </div>
</div>

<div class="core-feature-icons col-10 mt-3 d-flex align-items-center">
  <div id="cooking-time">
    <i class="fa fa-clock-o fa-2x mr-2"></i>
    <label for="cooking-time-text">{{recipe.cookingTime}}</label>
  </div>
  <div id="servings">
    <i class="fa fa-user fa-2x"></i>
    <label for="serving-text">{{recipe.servings}}</label>
  </div>
</div>

<div class="l-content d-flex align-items-center">
  <div id="recipe-content-left" class="l-col-left">
    <div id="ingredients">
      <h3>Ingredients</h3>
      <ul>
        {{#each recipe.ingredients as |ingredient|}}
        <li>{{ingredient.name}} {{ingredient.amount}} {{ingredient.unit}}</li>
        {{/each}}
      </ul>
    </div>
  </div>
  <div id="recipe-content-right" class="l-col-right">
    <div id="directions">
      <h3>Directions</h3>
      <div id="directions">{{{recipe.directions}}}</div>
    </div>
  </div>
</div>

<div>
  <form action="/recipes/{{recipe.id}}/ratings" method="POST">
    <input type="number" max="5" min="1" name="rate"/>
    <textarea name="review"></textarea>
    <button type="submit">Send</button>
  </form>
</div>
{{recipe.ratings}}

<h1>EEE {{average ratings=recipe.ratings index=1}}</h1>
<h1>EEE {{average ratings=recipe.ratings index=2}}</h1>
<h1>EEE {{average ratings=recipe.ratings index=3}}</h1>
<h1>EEE {{average ratings=recipe.ratings index=4}}</h1>
<h1>EEE {{average ratings=recipe.ratings index=5}}</h1>
 
<!--
<div class="row">
  <div class="col-3">
    <h1>{{recipe.title}}</h1>
    <img src="{{recipe.image}}" class="w-100 rounded border" />

  </div>

  <div class="col-9">
    <small>{{recipe.keyWords}}</small>
    {{{recipe.directions}}}
  </div>
</div> -->

<!-- aqui pueden ir recetas similares-->